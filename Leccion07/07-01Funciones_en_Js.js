//Declaracion de la funcion. (bloque de codigo no terminan en ;)
function miFuncion(a, b) {
    console.log("suma:" + (a + b));
}

// Llamando a la funcion (concepto de hosting)
miFuncion(2, 3);

miFuncion(10, 20);